requests==2.31.0
# If you want OBJ/GLTF handling libraries on the Modly side, add them here (e.g., trimesh, pygltflib)
# Note: To produce .blend files this script calls the system 'blender' CLI. Blender (not pip) must be
# installed and available in PATH in the environment where Modly runs this generator.
